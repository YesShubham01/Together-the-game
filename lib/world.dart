// import 'package:flame/components.dart';

// import 'Components/temp_player.dart';
// import 'Components/wall.dart';

// class GameWorld extends World {
//   @override
//   Future<void> onLoad() async {
//     super.onLoad();

//     // Add a Wall component with a visible color for debugging
//     add(Wall(
//       initialSize: 5000, // Adjust the size to something reasonable for testing
//       distanceFromPlayer: 10,
//       speed: 0.5,
//       startPosition: Vector2(0, 0), // Start it at a visible position
//       targetPosition: Vector2(10, 0), // A target within the screen bounds
//     ));

//     // Add the player with debug paint
//     add(TempPlayer(
//       position: Vector2(200, 200), // Start the player within the visible screen
//       size: Vector2(64, 64), // Reasonable player size
//     ));
//   }
// }
